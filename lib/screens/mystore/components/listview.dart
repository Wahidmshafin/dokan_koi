import 'package:flutter/material.dart';
class storedescription extends StatelessWidget {
  const storedescription({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Muri kha");
  }
}
